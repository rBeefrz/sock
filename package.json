{
  "name": "sock-tycoon",
  "version": "0.1.0",
  "description": "An idle sock empire game for the browser.",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "python3 -m http.server 8080"
  }
}
